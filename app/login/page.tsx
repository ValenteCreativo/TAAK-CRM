"use client";
import { useState } from "react";
export default function Login(){ const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false); async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});if(r.ok)location.href="/";else{setError("La contraseña no coincide");setLoading(false)}} return <main className="login"><form className="login-box" onSubmit={submit}><div className="login-logo">TAAK<span>·</span>CRM</div><div className="eyebrow">Espacio privado</div><h1 className="headline">Tu estudio, en orden.</h1><p className="muted">Entra a tu centro de proyectos y finanzas.</p><div className="field"><label>CONTRASEÑA</label><input autoFocus type="password" value={password} onChange={e=>setPassword(e.target.value)} /></div>{error&&<div className="error">{error}</div>}<button className="action" style={{width:"100%",marginTop:18}} disabled={loading}>{loading?"Entrando…":"Entrar al CRM"}</button></form></main> }
